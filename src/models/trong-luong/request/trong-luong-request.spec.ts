import { TrongLuongRequest } from './trong-luong-request';

describe('TrongLuongRequest', () => {
  it('should create an instance', () => {
    expect(new TrongLuongRequest()).toBeTruthy();
  });
});
